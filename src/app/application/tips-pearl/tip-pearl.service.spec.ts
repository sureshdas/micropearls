import { TestBed } from '@angular/core/testing';

// import { TipPearlService } from './tips-pearl.service';

describe('TipPearlService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    // const service: TipPearlService = TestBed.get(TipPearlService);
    // expect(service).toBeTruthy();
  });
});
